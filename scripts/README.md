# scripts/

Developer tooling. Scripts are cross-platform (bash, runnable from Git Bash on
Windows) and safe to run repeatedly. Added as milestones need them —
e.g. `generate-api-types` (M6), `seed-dev-data` (M2+).
